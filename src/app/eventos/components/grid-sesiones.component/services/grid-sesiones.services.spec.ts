// Auto-generated Jasmine spec for coverage

import * as Lib from './grid-sesiones.services';

describe('grid-sesiones.services', () => {
  it('should import module without errors', () => { expect(Lib).toBeDefined(); });
  it('should instantiate class Grid_sesionesService', () => {
    const C: any = (Lib as any)['Grid_sesionesService'];
    expect(C).toBeDefined();
    try { new C({} as any); } catch (_) { try { new C(); } catch { } }
  });
});