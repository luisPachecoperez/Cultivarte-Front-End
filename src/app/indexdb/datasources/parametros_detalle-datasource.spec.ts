// Auto-generated Jasmine spec for coverage

import * as Lib from './parametros_detalle-datasource';

describe('parametros_detalle-datasource', () => {
  it('should import module without errors', () => { expect(Lib).toBeDefined(); });
  it('should instantiate class Parametros_detalleDataSource', () => {
    const C: any = (Lib as any)['Parametros_detalleDataSource'];
    expect(C).toBeDefined();
    try { new C({} as any); } catch (_) { try { new C(); } catch { } }
  });
});