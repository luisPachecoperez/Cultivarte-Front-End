// Auto-generated Jasmine spec for coverage

import * as Lib from './personas_grupo_interes-datasource';

describe('personas_grupo_interes-datasource', () => {
  it('should import module without errors', () => { expect(Lib).toBeDefined(); });
  it('should instantiate class Personas_grupo_interesDataSource', () => {
    const C: any = (Lib as any)['Personas_grupo_interesDataSource'];
    expect(C).toBeDefined();
    try { new C({} as any); } catch (_) { try { new C(); } catch { } }
  });
});